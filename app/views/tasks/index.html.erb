<h1>My Tasks</h1>

<form action="">
  <% @tasks.each do |task| %>
    <input type="checkbox" id="<%= task.id %>">
    <label for="<%= task.id %>"><%= link_to task.title, task_path(task) %></label>
  <% end %>
</form>
